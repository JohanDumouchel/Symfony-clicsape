{{ form_start(form) }}
    <div class="col-lg-12 form-errors">
        {{ form_errors(form) }}
    </div>
    <div class="col-lg-3">
        <div class="form-group ">
            {{ form_row(form.title) }}
        </div>
        <div class="form-group">
            {{ form_row(form.level) }}
        </div>
        <div class="form-group">
            {{ form_row(form.file) }}
        </div>
        <div>
            <button type="submit" class="btn btn-primary">Ajouter</button>
        </div>
    </div>
{{ form_end(form) }}
