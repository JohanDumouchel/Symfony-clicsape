{# Admin base Twig template #}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}"/>
        <link rel="stylesheet" href="{{ asset('css/admin.css') }}"/>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <script src="{{ asset('js/javascript.js') }}"></script>
        <script src="{{ asset('js/admin.js') }}"></script>
    </head>
    <body>
        {% import 'ClicSapeAdminBundle::Default/button.html.twig' as button %}
        <div class="container-fluid">
            <div class="row">
                <nav class="navbar navbar-default navbar-fixed-top">
                <div class="container-fluid">
                    <div class="navbar-header">
                        <a class="navbar-brand" href="#">Admin</a>
                    </div>
                    <div class="collapse navbar-collapse">
                        <ul class="nav navbar-nav">
                            <li><a href="{{path('category_list')}}">Catégorie </a></li>
                            <li><a href="{{path('country_list')}}">Pays</a></li>
                            <li><a href="{{path('groupsize_list')}}">Group de taille</a></li>
                            <li><a href="{{path('article_list')}}">Article</a></li>
                    </div>
                </div>
            </div>
            <div class="row well">
                {% block h1 %}
                {% endblock %}
            </div>
            <div class="row">
                {% block body %}
                {% endblock %}
            </div>
        </div>
        <div class="loader"></div>
        <script src="{{ asset('js/admin/ready.js') }}"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
